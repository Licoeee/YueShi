<view class="role-page">
  <view class="role-page__background"></view>
  <role-page-scene
    scenePath="/pages/admin/profile/profile"
    activeRole="{{activeRole}}"
    isPreviewMode="{{isPreviewMode}}"
    roleSwitchOptions="{{roleSwitchOptions}}"
    bind:sceneaction="handleSceneAction"
  ></role-page-scene>

  <custom-tab-bar role="{{activeRole}}" currentPath="/pages/admin/profile/profile"></custom-tab-bar>
</view>
