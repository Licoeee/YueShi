<view class="role-page">
  <view class="role-page__background"></view>
  <role-page-scene
    scenePath="/pages/merchant/profile/profile"
    activeRole="{{activeRole}}"
    isPreviewMode="{{isPreviewMode}}"
    canBackToAdmin="{{canBackToAdmin}}"
    bind:sceneaction="handleSceneAction"
  ></role-page-scene>

  <custom-tab-bar role="{{activeRole}}" currentPath="/pages/merchant/profile/profile"></custom-tab-bar>
</view>
