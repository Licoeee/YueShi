# Welcome Screen 2.1 Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 交付 TODO 2.1 欢迎页视觉与动效，包括独立欢迎页、蛋糕主视觉、CTA 入场动画、圆形 reveal 转场，以及跳转到当前主页占位页。

**Architecture:** 新建 `pages/welcome` 作为应用首屏，把可测试的动画配置与粒子数据提取到纯 TypeScript 工具模块，页面文件只负责状态切换、定时调度和跳转。视觉结构由 WXML/WXSS 直接实现，避免图片和 Lottie 依赖，同时保持 TDesign CTA 语义。

**Tech Stack:** WeChat Mini Program, TypeScript, WXML, WXSS, TDesign Mini Program, Node.js test runner via `tsx`

---

## Chunk 1: Spec Lock And Testable Motion Primitives

### Task 1: 同步欢迎页设计文档与纯逻辑测试入口

**Files:**
- Modify: `docs/superpowers/specs/2026-03-11-welcome-screen-design.md`
- Create: `App/miniprogram/utils/welcome-motion.ts`
- Create: `App/tests/welcome-motion.test.ts`

- [ ] **Step 1: 写下第一个失败测试**

在 `App/tests/welcome-motion.test.ts` 中断言 `getWelcomeEntryTarget()` 返回 `pages/index/index`，并断言 `createRevealParticles()` 返回固定数量、合法位置区间和可渲染的粒子配置。

- [ ] **Step 2: 运行测试并确认失败**

Run: `npx -y -p tsx tsx --test App/tests/welcome-motion.test.ts`
Expected: FAIL，因为 `App/miniprogram/utils/welcome-motion.ts` 尚不存在。

- [ ] **Step 3: 写最小实现**

在 `App/miniprogram/utils/welcome-motion.ts` 中实现：
- `getWelcomeEntryTarget(): string`
- `createRevealParticles(): WelcomeParticle[]`
- `WELCOME_TIMINGS` 常量，集中定义按钮入场和 reveal 的延时。

- [ ] **Step 4: 再次运行测试并确认通过**

Run: `npx -y -p tsx tsx --test App/tests/welcome-motion.test.ts`
Expected: PASS。

## Chunk 2: Launch Entry And Static Welcome Layout

### Task 2: 建立独立欢迎页路由与基础状态

**Files:**
- Modify: `App/miniprogram/app.json`
- Create: `App/miniprogram/pages/welcome/welcome.json`
- Create: `App/miniprogram/pages/welcome/welcome.ts`

- [ ] **Step 1: 先写状态调度失败测试**

在 `App/tests/welcome-motion.test.ts` 追加 `getInitialWelcomeState()` 的断言，要求初始状态中按钮不可点击、未进入 reveal，且粒子配置已准备好。

- [ ] **Step 2: 运行测试并确认失败**

Run: `npx -y -p tsx tsx --test App/tests/welcome-motion.test.ts`
Expected: FAIL，因为 `getInitialWelcomeState()` 尚未实现。

- [ ] **Step 3: 实现最小状态工厂**

在 `App/miniprogram/utils/welcome-motion.ts` 中实现 `getInitialWelcomeState()`，然后在 `App/miniprogram/pages/welcome/welcome.ts` 定义 `WelcomePageData` 并使用该工厂初始化页面数据。

- [ ] **Step 4: 注册欢迎页为首屏**

把 `pages/welcome/welcome` 调整到 `App/miniprogram/app.json` 的首位，并在 `welcome.json` 中只注册 `t-button`。

- [ ] **Step 5: 重新运行测试**

Run: `npx -y -p tsx tsx --test App/tests/welcome-motion.test.ts`
Expected: PASS。

- [ ] **Step 6: 验证页面入口顺序**

Run: `Get-Content -Raw App\\miniprogram\\app.json`
Expected: `pages/welcome/welcome` 位于 `pages/index/index` 之前。

### Task 3: 搭建静态欢迎页结构

**Files:**
- Create: `App/miniprogram/pages/welcome/welcome.wxml`
- Create: `App/miniprogram/pages/welcome/welcome.wxss`

- [ ] **Step 1: 搭建标题区**

在 `welcome.wxml` 中创建标题容器，渲染 `悦时` 和 `遇见你的专属甜蜜时刻`，为后续半通透阴影字效预留分层 class。

- [ ] **Step 2: 搭建蛋糕视觉结构**

用纯 WXML 结构拆出蛋糕胚、奶油、三根蜡烛、火苗和彩色糖果，不引入图片、SVG 文件或 `t-icon`。

- [ ] **Step 3: 接入 CTA 和 reveal 容器**

在底部安全区加入 `t-button`，同时预留 reveal 覆盖层和粒子层的渲染节点。

- [ ] **Step 4: 写基础样式**

在 `welcome.wxss` 中实现固定背景层、内容层、标题视觉、蛋糕造型和 CTA 静态品牌样式。

- [ ] **Step 5: 检查静态页面结构**

Run: `Get-Content -Raw App\\miniprogram\\pages\\welcome\\welcome.wxml`
Expected: 同时包含标题块、蛋糕块、CTA 块和 reveal/particle 容器。

## Chunk 3: Intro Motion, Reveal, And Verification

### Task 4: 实现欢迎页入场动画和点击转场

**Files:**
- Modify: `App/miniprogram/pages/welcome/welcome.ts`
- Modify: `App/miniprogram/pages/welcome/welcome.wxml`
- Modify: `App/miniprogram/pages/welcome/welcome.wxss`

- [ ] **Step 1: 先写点击门禁失败测试**

在 `App/tests/welcome-motion.test.ts` 追加 `canStartReveal()` 的断言：按钮未就绪时返回 `false`，按钮就绪且未 reveal 时返回 `true`，reveal 进行中时返回 `false`。

- [ ] **Step 2: 运行测试并确认失败**

Run: `npx -y -p tsx tsx --test App/tests/welcome-motion.test.ts`
Expected: FAIL，因为 `canStartReveal()` 尚未实现。

- [ ] **Step 3: 实现最小门禁逻辑**

在 `App/miniprogram/utils/welcome-motion.ts` 中实现 `canStartReveal()`，并在 `welcome.ts` 中接入页面生命周期，使用 `WELCOME_TIMINGS` 驱动按钮从中心向四周展开，再在点击时锁定按钮并切换 reveal 状态。

- [ ] **Step 4: 完成动画样式**

在 `welcome.wxss` 中补齐蛋糕漂浮、火苗呼吸、按钮 materialize、圆形 reveal 和粒子拖尾动画。

- [ ] **Step 5: 再次运行测试并确认通过**

Run: `npx -y -p tsx tsx --test App/tests/welcome-motion.test.ts`
Expected: PASS。

### Task 5: 完成类型校验和人工验收准备

**Files:**
- Modify: None

- [ ] **Step 1: 运行 TypeScript 校验**

Run: `npx -y -p typescript tsc --noEmit -p App\\tsconfig.json`
Expected: exit code 0。

- [ ] **Step 2: 检查改动范围**

Run: `git status --short`
Expected: 仅出现欢迎页相关文件、测试文件和同步文档。

- [ ] **Step 3: 记录微信开发者工具验证步骤**

在最终交付说明中给出微信开发者工具 `2.01.2510280` 的操作步骤和预期效果，覆盖首屏进入欢迎页、蛋糕漂浮、火苗呼吸、按钮中心展开、点击 reveal 和跳转到 `pages/index/index`。
