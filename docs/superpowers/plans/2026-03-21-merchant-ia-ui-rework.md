# 商家端问题修复 + 信息架构调整 + 界面重做（两阶段）实施计划

> 创建时间：2026-03-21
> 约束：第一阶段仅审查与原型，不修改小程序界面代码；第二阶段待大帅确认后执行。

## 0. 目标与边界

- 目标：修复商家端关键报错与交互异常，重建商家端信息架构（订单管理、商品管理、商品库存、账本、风控）并完成统一视觉升级。
- 边界：不改无关模块，不破坏顾客端和管理员端既有能力；仅在需求所涉链路内最小改动。
- 技术约束：TypeScript 严格模式，禁止 `any`；UI 优先 TDesign；按钮变量改动前必须核对真实前缀；兼容微信开发者工具 2.01.2510280 / 基础库 3.14.3。

## Phase 1（当前执行）：审查 + 根因定位 + 本地高保真方案

### Task 1：现状审查（代码与信息架构）

- [x] 读取并对齐 `AGENTS.md`、`docs/STYLE_GUIDE.md`、`docs/PRD.md`、`docs/TODO.md`
- [x] 盘点商家端现有页面/组件/存储：订单、商品、账本、库存、黑名单
- [x] 审查排版层级、按钮语义、弹窗反馈、命名一致性与跨页风格统一性

### Task 2：问题根因定位（systematic-debugging）

- [x] 复现并定位订单管理 `t-calendar` 报错链路
- [x] 提供根因证据（调用方参数、组件期望类型、崩溃触发点）
- [x] 输出修复策略与回归风险（暂不改代码）

### Task 3：本地浏览器高保真静态稿

- [x] 产出方案 A：运营仪表卡片流
- [x] 产出方案 B：分栏运营台
- [x] 覆盖页面：订单管理、商品管理、商品回收站、商品库存、账本（资金流水）、关键弹窗与按钮状态
- [ ] 等待大帅确认方案

## Phase 2（待确认后执行）：按确认方案实施代码

### Chunk A：订单管理（4.1 + 新增修复项）

- [ ] 修复 `t-calendar` 报错根因（`format` 传值类型修正，不绕过）
- [ ] 修复日历弹层可视区域与定位问题
- [ ] 文案统一：`订单流水线 -> 订单管理`、`日历筛选 -> 选择日期`
- [ ] 去除本页小字说明，保留必要状态提示

### Chunk B：商品管理（4.2 + UI 重做）

- [ ] 改为与顾客端一致的瀑布流商品卡布局
- [ ] 卡片底部增加 `编辑 / 删除`
- [ ] 顶部动作区重排：`新增商品`、`商品回收站`、`批量编辑/退出修改`
- [ ] 新增商品改为弹窗流程（不再下拉展开）
- [ ] 新增商品支持上传图片
- [ ] 批量文案更新：`进入批量 -> 批量编辑`、`批量应用 -> 批量修改`、`退出批量 -> 退出修改`
- [ ] 批量修改新增奶油选项：`动物奶油i`、`乳脂奶油`（保留原文并记录风险）
- [ ] 清理本页小字说明

### Chunk C：商品 ID 与回收站稳态

- [ ] 引入递增 ID 生成器（`A001` 起，单独序列持久化，永不回退）
- [ ] 校验新增/删除/恢复/过期清理不产生重复 ID
- [ ] 保证与顾客端 ID 搜索口径兼容

### Chunk D：账本与库存信息架构重组

- [ ] 账本改为“资金流水”视图（收入/支出、日周月切换、趋势折线图）
- [ ] 将库存/散货记录能力整体迁移至“商品库存”
- [ ] 商品库存入库表单新增：生产日期（年/月/日下拉）、保质期天数、过期日期自动计算
- [ ] 入库列表展示图片 + 核心字段

### Chunk E：临期提醒订阅链路重构

- [ ] 移除界面上的“订阅临期提醒”按钮
- [ ] 修复订阅调用失败链路与时机
- [ ] 改为“首次成功入库后询问授权”；同意后持久化；拒绝后下次成功入库再询问
- [ ] 恢复逻辑：重进页面后按持久化偏好生效

### Chunk F：风控与门禁回归

- [ ] 黑名单页与 OpenID 拉黑能力回归验证
- [ ] 顾客提交订单路径拦截回归验证

### Chunk G：统一清理与回归

- [ ] 统一移除商家端页面小字说明文案
- [ ] 全量自动化测试 + 关键路径手工回归清单
- [ ] 若有临时打印，全部移除后再交付
- [ ] 输出“操作步骤 + 预期效果 + 实际结果”证据

## 验收门

- Gate 1（进入 Phase 2 前）：大帅确认静态稿方案
- Gate 2（提交前）：自动化回归通过 + 微信开发者工具人工回归证据完整
